# 常见问题

## DNS 解析失败 / Host lookup

1. 先用系统浏览器确认域名可访问
2. 必要时改填 IP 或切换 http/端口（例如 8096/8920）

## 播放 404

先确认服务端网页端能正常播放同一条目，再检查：
- 线路是否正确
- 是否需要切换主线路 / CDN 线路

## 批量导入解析不到地址

确认分享文本包含：
- `http(s)://...` URL，或
- 域名 / IP（也支持无 scheme，例如 `example.com 443`）

文档：`/SERVER_IMPORT`

